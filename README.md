# 🐳 Docker Nginx Static Site

This project demonstrates the basics of containerization using **Docker Compose** and **Nginx**. 🚀

🔹 **Goal:** Deploying a lightweight web server with minimal configuration.
🔹 **Tech:** Uses `volumes` to map local HTML files to the container – changes are reflected instantly without rebuilding! 🔄
🔹 **Network:** The application is accessible on port `8080`, mapped to the internal port `80`. 🌐
🔹 **Foundation:** A perfect starting point for understanding "Infrastructure as Code" concepts. 🛠️

---
**How to run:**
`docker-compose up -d`
